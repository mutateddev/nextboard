import { PersonStandingIcon } from 'lucide-react';

const MenuTitle = () => {
  return (
    <h4 className='flex items-center text-2xl font-bold'>
      <PersonStandingIcon className='text-primary' size={40} /> NextBoard
    </h4>
  );
};

export default MenuTitle;
